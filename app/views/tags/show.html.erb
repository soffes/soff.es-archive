<%- title "Tag - #{@tag.name}" -%>

<%- if @posts && @posts.length > 0 -%>
<%= render @posts %>
<div class="pagination">
    <%= will_paginate @posts %>
</div>
<%- else -%>
<p>Nothing to see here. Move along.</p>
<%- end -%>
